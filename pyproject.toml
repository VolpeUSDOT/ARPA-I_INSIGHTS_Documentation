[project]
name = "arpa-i-insights-documentation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "boto3>=1.43.44",
    "contextily>=1.7.1",
    "folium>=0.20.0",
    "geopandas>=1.1.4",
    "ipywidgets>=8.1.8",
    "jupyter>=1.1.1",
    "laspy>=2.7.0",
    "lazrs>=0.8.1",
    "mapclassify>=2.8.1",
    "matplotlib>=3.10.9",
    "numpy>=2.2.6",
    "pandas>=2.3.3",
    "pdal>=3.5.4",
    "pyarrow>=24.0.0",
    # Imported directly by the notebook and examples/scripts (and required by
    # laspy's CRS handling), so declared rather than relied on transitively.
    "pyproj>=3.7.1",
    "shapely>=2.0",
]
